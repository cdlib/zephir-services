language: python
python:
  - '3.6'
install:
  - 'pip install --user codecov'
  - 'pip install pipenv'
  - 'pipenv install'
script:
  - 'pipenv run pytest'
after_success:
    - bash <(curl -s https://codecov.io/bash)
